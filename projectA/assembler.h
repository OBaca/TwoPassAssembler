
/*get_file, opens a file */
FILE* get_file(char *, int, char *, char *);

/*create_new_file - is a function that gets a file name and change the ending of the name depends on the ending name given. */
char *create_new_file(char *, char *, int, int);

/* */
void manage(char *, int);

